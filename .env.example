# API 基础前缀，会被 axios 用作 baseURL（生产可改成后端实际域名）
VITE_API_BASE_URL=/api

# 是否启用 Mock.js 拦截（仅开发环境生效），true=启用，false=走后端或 vite proxy
VITE_USE_MOCK=true

# Vite dev server proxy 目标地址（当 VITE_USE_MOCK=false 时生效，将 /api 转发到这里）
# 例如：http://127.0.0.1:8080
VITE_PROXY_TARGET=http://127.0.0.1:8080
